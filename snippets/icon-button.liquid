<span
  class="icon-button icon-button-{{ newIcon }}{{icon}} {% if small %} icon-button--small{% endif %} {{ class }}"
  {% if hidden %}
    aria-hidden hidden
  {% endif %}
>
  <span class="icon-button__icon">
    {% render 'icon' with icon: icon %}
  </span>
</span>
